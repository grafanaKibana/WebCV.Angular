import { MeshGradientDirective } from './mesh-gradient.directive';

describe('MeshGradientDirective', () => {
  it('should create an instance', () => {
    const directive = new MeshGradientDirective();
    expect(directive).toBeTruthy();
  });
});
